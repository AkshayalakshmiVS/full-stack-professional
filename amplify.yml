version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend/react
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend/react/build   # React build output
    files:
      - '**/*'
  cache:
    paths:
      - frontend/react/node_modules/**/* 

