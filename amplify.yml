version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd react
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: react/build   # or wherever your build output goes
    files:
      - '**/*'
  cache:
    paths:
      - react/node_modules/**/* 
